import { createTheme } from '@material-ui/core/styles';

export const theme = createTheme({
  palette: {
    background: {
      paper: '#fff',
      default: '#eee',
      level2: '#f5f5f5',
      level1: '#fff',
    },
  },
});
